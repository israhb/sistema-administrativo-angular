import { RolesPermisosHtmlDirective } from './roles-permisos-html.directive';

describe('RolesPermisosHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new RolesPermisosHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
