import { RolesPermisosDirective } from './roles-permisos.directive';

describe('RolesPermisosDirective', () => {
  it('should create an instance', () => {
    const directive = new RolesPermisosDirective();
    expect(directive).toBeTruthy();
  });
});
