import { RolesModulosDirective } from './roles-modulos.directive';

describe('RolesModulosDirective', () => {
  it('should create an instance', () => {
    const directive = new RolesModulosDirective();
    expect(directive).toBeTruthy();
  });
});
