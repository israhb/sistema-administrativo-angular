import { FieldValidationPipe } from './field-validation.pipe';

describe('FieldValidationPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldValidationPipe();
    expect(pipe).toBeTruthy();
  });
});
